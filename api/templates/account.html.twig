{% extends 'public/base.html.twig' %}
{% block title %}Your Account{% endblock %}
{% block body %}
    {% include 'public/navbar.html.twig' %}
    <div class="container-fluid d-flex flex-column justify-content-start p-4 mt-5 pt-5" style="min-height: calc(100vh - 56px);">
        <div class="row w-100">
            <div class="col-lg-8 col-md-10 mx-auto">
                <div class="card shadow-lg">
                    <div class="card-body">
                        <div class="row">
                            <!-- Avatar & Nom à gauche -->
                            <div class="col-md-4 text-center mb-3">
                                <img src="{{ loginData.user.picture }}" alt="Avatar de KindaRusty" class="rounded-circle img-fluid" style="max-width: 150px;">
                                <h4>KindaRusty</h4>
                                <p class="text-muted">@minedefer314</p>
                            </div>

                            <!-- Contenu à droite -->
                            <div class="col-md-8">
                                <h5>Informations</h5>
                                <p>Autres informations du profil ici...</p>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card shadow-lg">
                    <div class="card-body">
                        <div class="row">
                            <!-- Avatar & Nom à gauche -->
                            <div class="col-md-4 text-center mb-3">
                                <img src="{{ loginData.user.picture }}" alt="Avatar de KindaRusty" class="rounded-circle img-fluid" style="max-width: 150px;">
                                <h4>KindaRusty</h4>
                                <p class="text-muted">@minedefer314</p>
                            </div>

                            <!-- Contenu à droite -->
                            <div class="col-md-8">
                                <h5>Informations</h5>
                                <p>Autres informations du profil ici...</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}